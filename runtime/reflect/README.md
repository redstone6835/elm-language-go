# 反射

本项目只支持 AOT 镜像中保留的静态反射元数据。构建插件根据显式 roots 和 SDK 使用情况裁剪
类型、方法与字段表。

不支持：

- `plugin` 和运行时装载 Go package；
- 运行时生成机器码；
- 通过反射访问未保留的私有内核 ABI；
- 依赖文件系统或宿主进程信息的反射扩展。

反射元数据是签名 artifact 的一部分，必须计入 package digest 与内存预算。
