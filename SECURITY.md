# 安全边界

Go ELM 与 Rust ELM 一样运行在内核地址空间，是受信任模块。签名、静态检查、能力清单和 AOT
不能把错误的指针、数据竞争或越界 MMIO 变成安全代码；它们用于阻止不兼容或未经授权的模块
进入内核，并缩小可审计范围。

提交安全问题时，请私下联系仓库维护者，不要先公开可复现的内核内存破坏方法。报告至少包含
受影响 revision、目标架构、工具链锁版本、最小复现和影响范围。

以下情况属于设计违规：

- 绕过 EKI Profile 或签名校验直接链接内核私有符号；
- 在 carrier 边界保存 Go heap 指针或让其跨 generation 存活；
- 在 IRQ 顶半部触发分配、GC 或阻塞；
- 卸载后仍有 goroutine、回调、DMA 或 IRQ 可进入旧代码；
- 把 managed dispatcher 描述为不可信代码的隔离边界；
- 未锁定或未校验来源的编译器、patch 或二进制工具链。
